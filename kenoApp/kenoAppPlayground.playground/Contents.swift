import UIKit

/*func numberGenerator() -> [Int] {
    var numbers: [Int] = []
    var newNum: Int
    
    while numbers.count < 20 {
        newNum = Int.random(in: 1 ... 80)
        if !numbers.contains(newNum){
            numbers.append(newNum)
        }
    }
    
    print(numbers)
    
    return numbers
}

var testNumbers = numberGenerator()
print(testNumbers)
*/


